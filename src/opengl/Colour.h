/* The color of the body */
typedef struct Colors
{
    float r;    /* The red value   */
    float g;    /* The green value */
    float b;    /* The blue value  */
    float a;    /* The alpha value */
}Color;

Color getNewColor(float, float, float, float);
